node_modules
dist
build
coverage
.DS_Store
*.log
.turbo
.vite
.cache
.vercel
.env
.env.local
.env.*.local

# 本機個人 IDE / agent 設定（不該共享）
.claude/
.cursor/
.idea/
.vscode/

# 部署密碼 / server 變數（用 env 注入，不入 git）
.env.deploy
.env.deploy.local

# 部署文件 / 腳本（含 k8s manifest 與 SSH/kubectl deploy script，不推 GitHub）
/deploy/
/scripts/deploy-docs.sh
/scripts/deploy-docs-kubectl.sh

# npm pack 暫存（smoke-published.mjs 會產，正常情況自己清，crash 時遺留兜底）
*.tgz

# Playwright artifacts
test-results/
playwright-report/
playwright/.cache/

# Mobile review screenshots（scripts/mobile-review.mjs 產，僅本機評估用）
mobile-review/
